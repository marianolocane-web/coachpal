Desplegable simple para elegir entre pocas opciones (frecuencia, categoría, recordatorio).

```jsx
<Select label="Frecuencia" value={freq} onChange={setFreq} options={[{value:'daily',label:'Todos los días'},{value:'weekdays',label:'Días laborables'}]} />
```
