Botón circular solo-icono para acciones secundarias en headers y filas (editar, cerrar, más opciones).

```jsx
<IconButton icon={<XIcon size={20}/>} label="Cerrar" variant="ghost" />
```

Variantes `secondary` / `ghost` / `brand`. Tamaños `sm` (32) / `md` (40) / `lg` (48).
