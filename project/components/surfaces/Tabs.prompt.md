Selector de segmentos tipo pill para alternar vistas (Semana/Mes/Año en el historial).

```jsx
<Tabs value={range} onChange={setRange} tabs={[{value:'week',label:'Semana'},{value:'month',label:'Mes'}]} />
```
